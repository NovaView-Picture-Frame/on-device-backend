device:
  screen:
    width: 1920
    height: 1080

server:
  port: 3000
  data_dir: data
  database: data.sqlite

runtime:
  tasks_results_ttl_ms: 600000
  websocket_heartbeat_interval_ms: 30000
  websocket_heartbeat_timeout_ms: 10000
  websocket_heartbeat_retries: 2

services:
  upload:
    timeout_ms: 10000
    size_limit_bytes: 15728640

  query:
    default_page_size: 50
    max_page_size: 100

  preview:
    max_width: 1920
    max_height: 1080

  carousel:
    default_order: random
    default_interval_ms: 5000
    acceptable_delay_ms: 2000
    schedule_window_size: 24

external:
  nominatim:
    user_agent: on-device-backend
